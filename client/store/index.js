// const tinyCookie = require('tiny-cookie')

// export const state = () => ({
//     isLoggedIn: tinyCookie.getCookie('isLoggedIn') !== null && tinyCookie.getCookie('isLoggedIn') === 'true' ? true : false
// })

// export const mutations = {
//     login(state) {
//         state.isLoggedIn = true
//         tinyCookie.setCookie('isLoggedIn', 'true')
//     },

//     logout(state) {
//         state.isLoggedIn = false
//     }
// }

export const state = () => {}

export const mutations = {}

export const actions = {}

export const getters = {}