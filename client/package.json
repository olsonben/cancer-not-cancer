{
  "name": "client",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt dev --dotenv .env.development",
    "staging": "NODE_OPTIONS=--openssl-legacy-provider nuxt start --dotenv .env.staging --port $PORT",
    "sim": "nuxt dev --dotenv .env.simulation",
    "generateDev": "nuxt generate --dotenv .env.development",
    "generate": "NODE_OPTIONS=--openssl-legacy-provider nuxt generate --dotenv .env.production",
    "generateStaging": "NODE_OPTIONS=--openssl-legacy-provider nuxt generate --dotenv .env.staging",
    "generateSim": "NODE_OPTIONS=--openssl-legacy-provider nuxt generate --dotenv .env.simulation"
  },
  "devDependencies": {
    "@fortawesome/fontawesome-svg-core": "^6.5.1",
    "@fortawesome/free-regular-svg-icons": "^6.5.1",
    "@fortawesome/free-solid-svg-icons": "^6.5.1",
    "@fortawesome/vue-fontawesome": "^3.0.5",
    "@nuxt/devtools": "latest",
    "@pinia/nuxt": "^0.5.1",
    "bulma": "^0.9.4",
    "form-data": "^4.0.0",
    "kill-port": "^2.0.0",
    "nuxt": "^3.8.0",
    "pinia": "^2.1.7",
    "sass": "^1.52.1",
    "vue": "^3.3.7",
    "vue-matomo": "^4.2.0",
    "vue-router": "^4.2.5"
  }
}
